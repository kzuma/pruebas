mcp[cli]>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Redis — used when REDIS_URL is set (Cloud Run / Cloud Memorystore)
redis>=5.0.0

# Google Auth — used when CLOUD_RUN_ENV=true for identity tokens
google-auth>=2.36.0
